@model JTX_Ecom.Models.DTOs.LoginDto

@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card shadow login-card">
                <div class="card-body p-5">
                    <div class="text-center mb-4">
                        <h2 class="fw-bold text-dark">Welcome Back</h2>
                        <p class="text-secondary fw-semibold">Login to your account</p>
                    </div>

                    @* Only show success messages that are registration-related *@
                    @if (TempData["SuccessMessage"] != null)
                    {
                        var message = TempData["SuccessMessage"]?.ToString() ?? "";
                        // Only show registration success or password reset messages
                        if (message.Contains("Registration successful") || 
                            message.Contains("Password") || 
                            message.Contains("account"))
                        {
                            <div class="alert alert-success alert-dismissible fade show">
                                @message
                                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                            </div>
                        }
                    }
                    
                    @* Show info message if user needs to login to continue *@
                    @if (ViewData["ReturnUrl"] != null && !string.IsNullOrEmpty(ViewData["ReturnUrl"]?.ToString()))
                    {
                        <div class="alert alert-info alert-dismissible fade show">
                            <i class="bi bi-info-circle"></i> Please log in to continue.
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    <form asp-action="Login" method="post">
                        <input type="hidden" name="ReturnUrl" value="@ViewData["ReturnUrl"]" />
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Email" class="form-label fw-bold text-dark">Email Address</label>
                            <input asp-for="Email" class="form-control form-control-lg" placeholder="Enter your email" />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Password" class="form-label fw-bold text-dark">Password</label>
                            <input asp-for="Password" type="password" class="form-control form-control-lg" 
                                   placeholder="Enter your password" />
                            <span asp-validation-for="Password" class="text-danger"></span>
                        </div>

                        <div class="d-grid mb-3">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="bi bi-box-arrow-in-right"></i> Login
                            </button>
                        </div>

                        <div class="text-center">
                            <p class="mb-0 text-dark fw-semibold">
                                Don't have an account? 
                                <a asp-action="Register" class="text-decoration-none fw-bold text-primary">Sign up here</a>
                            </p>
                        </div>
                    </form>

                    <hr class="my-4">

                    <div class="text-center">
                        <small class="text-secondary fw-semibold">
                            <strong>Admin Credentials:</strong><br>
                            Email: admin@jtxconcert.com<br>
                            Password: Admin@123
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
<style>
    .login-card {
        border: none;
        border-radius: 15px;
        background: #ffffff !important;
    }
    
    .login-card .card-body {
        background: #ffffff !important;
    }
    
    .text-dark {
        color: #2c3e50 !important;
    }
    
    .text-secondary {
        color: #6c757d !important;
    }
    
    .text-primary {
        color: #667eea !important;
    }
    
    h2 {
        color: #2c3e50 !important;
    }
    
    .form-label {
        color: #2c3e50 !important;
    }
    
    .form-control {
        background: #f8f9fa;
        border: 2px solid #e9ecef;
        color: #2c3e50 !important;
        font-weight: 600;
    }
    
    .form-control:focus {
        border-color: #667eea;
        box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        color: #2c3e50 !important;
    }
    
    .form-control::placeholder {
        color: #95a5a6 !important;
    }

    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        font-weight: 700;
    }

    .btn-primary:hover {
        opacity: 0.9;
    }
</style>
}

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
